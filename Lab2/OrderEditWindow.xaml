<Window x:Class="CafeOrderApp.OrderEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование заказа" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        Icon ="Images\icon.ico"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Text="Имя клиента:" FontWeight="Bold"/>
            <TextBox x:Name="ClientNameTextBox" Text="{Binding Order.ClientName, Mode=TwoWay}" Margin="0,5,0,10"/>
            <TextBlock Text="Тип заказа:" FontWeight="Bold"/>
            <TextBox x:Name="OrderTypeTextBox" Text="{Binding Order.OrderType, Mode=TwoWay}" Margin="0,5,0,10"/>
        </StackPanel>
        <TextBlock Grid.Row="1" Text="Блюда в заказе:" FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>
        <ListBox x:Name="OrderItemsListBox" Grid.Row="1" Margin="0,30,0,0" 
         ItemsSource="{Binding Order.Items}" 
         SelectionMode="Single">
    <ListBox.ItemTemplate>
        <DataTemplate>
            <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="3" Padding="5" Margin="5">
                <StackPanel>
                    <TextBlock TextWrapping="Wrap" FontWeight="Bold">
                        <Run Text="{Binding Dish.Name}"/>
                        <Run Text=" ("/>
                        <Run Text="{Binding OptionsString, Mode=OneWay}"/>
                        <Run Text=")"/>
                    </TextBlock>
                    <TextBlock TextWrapping="Wrap">
                        <Run Text="Кол-во: "/>
                        <Run Text="{Binding Quantity}"/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </DataTemplate>
    </ListBox.ItemTemplate>
</ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Удалить блюдо" Click="RemoveDish_Click" Margin="0,0,5,0"/>
            <Button Content="Сохранить" Click="SaveOrder_Click"/>
        </StackPanel>
    </Grid>
</Window>
